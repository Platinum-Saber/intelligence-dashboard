# Copy to .env and adjust values

DEBUG=true
DATABASE_URL=sqlite:///./procurement_intel.db
SEED_DAYS=90

# For PostgreSQL (production):
# DATABASE_URL=postgresql://user:password@localhost:5432/procurement_intel

FRONTEND_URL=http://localhost:5173

# SMTP alert notifications (optional for Phase 1)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
ALERT_FROM_EMAIL=

# Phase 2: live data feed keys
# API_KEY_EXCHANGERATE=
# API_KEY_NEWSAPI=
# API_KEY_METALS=
